import {it,expect,vi} from 'vitest';
import {existsSync,readFileSync} from 'node:fs';
import {ITEMS,RunProgression} from '../src/run/RunProgression';
import {PERK_ICONS} from '../src/ui/PerkIcons';
import {EventBus} from '../src/core/EventBus';
import {RunRNG} from '../src/core/RunRNG';
import {ItemProcs} from '../src/items/ItemProcs';
import {MonsterDirector} from '../src/run/MonsterDirector';
import {PistolMagazine} from '../src/combat/PistolMagazine';
import {SkillTimeline,SKILL_CUES} from '../src/combat/SkillTimeline';
import {directionalLocomotion} from '../src/animation/DirectionalLocomotion';
import type {DamageContext} from '../src/core/contracts';
const hit:DamageContext={attackerId:1,victimId:2,sourceId:'pistol',attackId:'right',baseDamage:12,finalDamage:12,crit:false,procCoefficient:1,procChainDepth:0,damageTags:['bullet'],hitPosition:{x:0,y:0,z:0},hitNormal:{x:0,y:1,z:0},forceDirection:{x:0,y:0,z:1},forceMagnitude:1};
it('maps every supplied transparent perk to exactly one reachable definition',()=>{expect(ITEMS).toHaveLength(90);expect(new Set(ITEMS.map(i=>i.id)).size).toBe(90);expect(new Set(ITEMS.map(i=>i.icon)).size).toBe(90);for(const item of ITEMS){expect(existsSync('public/perks/'+PERK_ICONS[item.icon])).toBe(true);expect(item.stat||item.hook).toBeTruthy();}const rng=new RunRNG('all-rewards').stream('loot'),run=new RunProgression(new EventBus()),seen=new Set();for(let i=0;i<10000;i++)seen.add(run.randomItem(rng).id);expect(seen.size).toBe(90);});
it.each(ITEMS)('$name applies its effect and stacks three copies',item=>{const run=new RunProgression(new EventBus()),base=run.stats;run.addItem(item.id);const first=run.stats;run.addItem(item.id);run.addItem(item.id);expect(run.inventory.get(item.id)).toBe(3);if(item.stat){const stat=item.stat;expect(first[stat]).toBeGreaterThan(base[stat]);if(stat==='crit'){expect(run.stats.crit).toBeCloseTo(3*item.value!/(1+3*item.value!));}else expect(run.stats[stat]-base[stat]).toBeCloseTo(3*item.value!);}else{const rng=new RunRNG(item.id).stream('procs');vi.spyOn(rng,'next').mockReturnValue(0);const procs=new ItemProcs(run,rng),hooks={burn:vi.fn(),blast:vi.fn()};procs.onHit(hit,hooks);if(item.hook==='burn')expect(hooks.burn).toHaveBeenCalledWith(6);if(item.hook==='blast')expect(hooks.blast).toHaveBeenCalledWith(3.9);if(item.hook==='harvest')expect(procs.onKill()).toBe(12);}});
it('finishes ten hordes, rewards once each, bosses at five and ten, obeying a small cap',()=>{const d=new MonsterDirector(new RunRNG('waves').stream('director'),1,50,true);let population=0,kills=0,bossAlive=false;const bosses:number[]=[],strengths:number[]=[];let lastWave=0;for(let tick=0;tick<30000&&d.completedWaves<10;tick++){if(d.wave!==lastWave){strengths.push(d.healthMultiplier);lastWave=d.wave;}d.update(.1,kills,population,kind=>{population++;if(kind==='boss'){bossAlive=true;bosses.push(d.wave);}return true;},4);expect(population).toBeLessThanOrEqual(4);if(tick%9===0&&population){population--;kills++;if(bossAlive){bossAlive=false;d.bossKilled();}}}expect(d.completedWaves).toBe(10);expect(d.rewardsPending).toBe(10);expect(bosses).toEqual([5,10]);for(let i=1;i<strengths.length;i++)expect(strengths[i]).toBeGreaterThan(strengths[i-1]!);});
it('does not advance or reward when spawning fails or enemies remain',()=>{const d=new MonsterDirector(new RunRNG('failed').stream('director'),1,50,true);for(let i=0;i<1000;i++)d.update(.1,99,0,()=>false,4);expect(d.wave).toBe(1);expect(d.rewardsPending).toBe(0);d.spawned=d.waveQuota;for(let i=0;i<100;i++)d.update(.1,99,1,()=>false,4);expect(d.wave).toBe(1);});
it('fires exactly 50 rounds, blocks during reload and fills only at the catch',()=>{const m=new PistolMagazine();for(let i=0;i<50;i++)expect(m.consume()).toBe(true);expect(m.consume()).toBe(false);expect(m.request()).toBe(true);m.update(1);expect(m.consume()).toBe(false);expect(m.ammo).toBe(0);m.update(.35);expect(m.ammo).toBe(50);expect(m.reloading).toBe(false);expect(m.request()).toBe(false);m.consume();m.request();m.update(1);m.cancel();expect(m.ammo).toBe(49);});
it.each([1,2,3] as const)('skill %i releases once at its audio cue and pauses without drift',tier=>{const timeline=new SkillTimeline(),release=vi.fn();timeline.start(tier);timeline.update(SKILL_CUES[tier].release-.001,release);expect(release).not.toHaveBeenCalled();for(let i=0;i<60;i++)timeline.update(timeline.elapsed,release);expect(release).not.toHaveBeenCalled();expect(timeline.start(tier)).toBe(false);timeline.update(SKILL_CUES[tier].release,release);timeline.update(9,release);expect(release).toHaveBeenCalledExactlyOnceWith(tier);expect(timeline.active).toBe(false);});
it('selects backsteps and strafes relative to facing, blending diagonals',()=>{expect(directionalLocomotion(0,-3,0).primary).toBe('WalkBackward');expect(directionalLocomotion(-3,0,0).primary).toBe('StrafeLeft');expect(directionalLocomotion(3,0,0).primary).toBe('StrafeRight');expect(directionalLocomotion(-8,0,Math.PI/2).primary).toBe('RunBackward');expect(directionalLocomotion(3,-3,0).weight).toBeCloseTo(.5);});
it('authored movement, preparations and reload keep the supplied skin and textures',()=>{const glb=(file:string)=>{const b=readFileSync(file);return JSON.parse(b.toString('utf8',20,20+b.readUInt32LE(12)));},before=glb('art/processed/gunslinger-before-directional.glb'),after=glb('public/models/gunslinger.glb');for(const key of ['meshes','skins','images','materials','textures'])expect(after[key]).toEqual(before[key]);for(const name of ['WalkBackward','StrafeLeft','StrafeRight','RunBackward','RunStrafeLeft','RunStrafeRight','FanCast','PrepareFan','PrepareMortal','PrepareStorm','ReloadCast']){const a=after.animations.find((a:{name:string})=>a.name===name);expect(a).toBeTruthy();for(const s of a.samplers)expect(after.accessors[s.input].count).toBe(61);}});

it.each([1,2,3] as const)('skill %i ends at vocal cutoff even when the file has a longer tail',tier=>{const t=new SkillTimeline(),release=vi.fn();t.start(tier,6.034);const end=SKILL_CUES[tier].voiceEnd;expect(t.duration).toBe(end);t.update(1.5,release);expect(t.closeVisible).toBe(false);t.update(end-.001,release);expect(t.active).toBe(true);t.update(end,release);expect(t.active).toBe(false);expect(t.actionProgress).toBe(1);expect(release).toHaveBeenCalledTimes(1);});
it('replaces enemies retired for performance instead of counting them as defeated',()=>{const d=new MonsterDirector(new RunRNG('retire').stream('director'),1,50,true);d.intermission=0;d.spawned=d.waveQuota;d.retireLivingEnemy();for(let i=0;i<100;i++)d.update(.1,0,0,()=>false,12);expect(d.completedWaves).toBe(0);expect(d.spawned).toBe(d.waveQuota-1);let spawned=0;d.update(1,0,0,()=>{spawned++;return true;},12);expect(spawned).toBe(1);d.update(.1,0,1,()=>false,12);expect(d.completedWaves).toBe(0);d.update(.1,1,0,()=>false,12);expect(d.completedWaves).toBe(1);});

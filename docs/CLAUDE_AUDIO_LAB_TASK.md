# Prioridade do usuário: ouvir e substituir sons de cada inimigo

Pedido: "Quero ver o som de cada inimigo e conseguir substituir rapidamente tem uns sons que me inrrita".

## Divisão de autoria (evitar conflitos)
Claude do painel pode criar/editar SOMENTE `audio-lab.html`, `src/audio/EnemyAudioOverrides.ts`, `src/audio/EnemyAudioCatalog.ts`, `src/ui/EnemyAudioLab.ts`, `tests/enemy-audio-overrides.test.ts`, `docs/CLAUDE_AUDIO_LAB_DELIVERY.md`. Codex cria `src/ui/enemy-audio-lab.css` e revisa visualmente. Claude principal continua dono de `RecordedAudio.ts`, manifest e integração do jogo. Não editar esses arquivos. Expor API simples e escrever instruções de integração no DELIVERY assim que possível, antes de encerrar a tarefa.

## Entrega funcional
Página leve `/audio-lab.html` independente da cena 3D, em português, pronta para abrir imediatamente. Seletor de inimigo (berinjela, milho, melancia, tomate, cenoura e chefe), eventos de aparecer/preparar ataque/atacar/receber dano/morrer/esquivar e camada adicional do ataque (hoje WeaponAudio.enemy acrescenta pistol/swish/charge/heavy). Mostrar grupos/arquivos efetivamente resolvidos do manifest, todas as variações acessíveis, ouvir individual ou sequência com Parar. Nunca tocar automaticamente ao abrir. Controle de volume por evento, silenciar, substituir via input file com formatos decodificáveis, restaurar padrão. Decodificar antes de aceitar arquivo; erro claro sem perder configuração anterior. Escape fecha modal se houver; nomes de arquivos via textContent, não HTML arbitrário.

Persistência de escolhas e blobs em IndexedDB, aplicação sem recarregar pelo cliente do jogo via API e BroadcastChannel. Class/API EnemyAudioOverrides mantém cache pronto em memória para consulta síncrona na hora do evento; await ready fora do loop. Expor lookup(kind,event) e getBuffer(context,...) ou equivalente, mute/gain/override por evento, e notificar mudanças. Limpar recursos/nodes/URLs no dispose. Não bloquear loop de áudio ou causar rejeições não tratadas se storage indisponível; comunicar modo temporário. API distingue evento sem override de muted, sem silenciar todos os sons por engano. Carregamento e troca rápida A->B não pode deixar decode antigo vencer. Nada externo/upload. Arquivos permanecem locais, indicar 'Salvo neste navegador'. Exportar/importar escolhas COM os áudios personalizados (JSON autocontido é aceitável, validar limites/formato e preservar originais). Restaurar padrão por evento e espécie.

O catálogo deve resolver as chaves do foley-manifest atual (windup->growl, hit->hurt, fallback) e reproduzir também camada de ataque adicional. Usar uma única fonte de verdade exportada que o Claude principal possa usar no runtime, para o painel realmente representar o jogo. Novos sons padrão serão ajustados pelo principal: resolução por manifest, não congelar cópia velha dos URLs. Defaults rate/gain devem ter parâmetros explicados nas instruções de integração.

## Contrato visual para CSS do Codex
Body class `audio-lab`; root `.audio-lab-shell`; header `.audio-lab-header`, marca `.audio-lab-kicker`, h1, descrição e `.audio-lab-toolbar` (parar tudo/exportar/importar); `.audio-lab-layout` com nav `.audio-species` (botões aria-pressed) e main `.audio-events`. Cabeçalho `.audio-species-heading` com h2 e restore. Cada evento `.audio-event-card`, heading h3, descrição/status `.audio-event-status`, variações `.audio-variants`, cada `.audio-variant` com button ouvir e span nome (com quebra), controles `.audio-event-controls` com volume/mute/substituir/restaurar. Statusglobal `.audio-lab-status` role=status, `.audio-lab-error` role=alert. Estados muted/loading via data ou classe. Bons labels, foco teclado, sem dependência CSS na lógica. Importar './enemy-audio-lab.css' no módulo UI. HTML com title 'Estúdio de sons · Mutant Farm' e script type module. Pode ajustar nomes se necessário documentando.

## Verificação
Testes significativos do lookup/fallback/mute/restauração/validação import; typecheck de seus arquivos no projeto (pode haver mudanças concorrentes, registrar). Nenhum commit/push/reset/stash; não tocar arquivos de outros autores. Finalizar com arquivos + API de integração exata + limitações. Não apenas criar mock estático: controles e persistência devem funcionar.

import {readFileSync} from 'node:fs';import {NullEngine} from '@babylonjs/core/Engines/nullEngine.js';import {Scene} from '@babylonjs/core/scene.js';import {ImportMeshAsync} from '@babylonjs/core/Loading/sceneLoader.js';import '@babylonjs/loaders/glTF/index.js';
const e=new NullEngine(),s=new Scene(e);const m=await ImportMeshAsync(new Uint8Array(readFileSync('public/models/gunslinger.glb')),s,{pluginExtension:'.glb',pluginOptions:{gltf:{skipMaterials:true}}});for(const a of m.animationGroups)a.stop();
for(const name of ['Idle','Aim','PrepareMortal']){const a=m.animationGroups.find(a=>a.name===name);for(const t of a.targetedAnimations){const v=t.animation.evaluate(a.from+(a.to-a.from)*.94);if(t.animation.targetProperty==='rotationQuaternion')t.target.rotationQuaternion=v.clone();else if(t.animation.targetProperty==='position')t.target.position.copyFrom(v);else if(t.animation.targetProperty==='scaling')t.target.scaling.copyFrom(v);}for(const n of m.transformNodes)n.computeWorldMatrix(true);for(const skeleton of m.skeletons)skeleton.prepare(true);for(const mesh of m.meshes)if(mesh.getTotalVertices()){mesh.refreshBoundingInfo(true);console.log(name,mesh.name,mesh.getBoundingInfo().boundingBox.minimumWorld.asArray(),mesh.getBoundingInfo().boundingBox.maximumWorld.asArray());}}
s.dispose();e.dispose();

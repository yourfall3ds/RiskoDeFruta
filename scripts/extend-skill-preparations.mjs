import fs from 'node:fs';
let p='scripts/author-directional-animation.mjs',s=fs.readFileSync(p,'utf8');s=s.replace("['FanCast',1.05,0,0]", "['FanCast',1.05,0,0],['PrepareFan',1.2,0,0],['PrepareMortal',1.2,0,0],['PrepareStorm',1.2,0,0]");s=s.replace("const fan=name==='FanCast',run=", "const fan=name==='FanCast'||name.startsWith('Prepare'),posePhase=name.startsWith('Prepare')?phase*.48:phase,run=");s=s.replace('Math.sin(Math.PI*phase)**2','Math.sin(Math.PI*posePhase)**2').replace("-.24*Math.sin(phase*Math.PI*2)","-.24*Math.sin(posePhase*Math.PI*2)").replace('sign*(.12+.48*Math.sin(Math.PI*phase))',"sign*(name==='PrepareStorm'?-.32*pulse:.12+.48*Math.sin(Math.PI*posePhase))").replace('sign*.35*Math.sin(Math.PI*phase)','sign*.35*Math.sin(Math.PI*posePhase)');fs.writeFileSync(p,s);

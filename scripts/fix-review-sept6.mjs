import {readFileSync,writeFileSync,appendFileSync} from 'node:fs';
const edit=(p,a,b)=>{let s=readFileSync(p,'utf8');if(!s.includes(a))throw Error(p+' missing '+a.slice(0,60));writeFileSync(p,s.replace(a,b));};
appendFileSync('docs/OBJECTIVES_QUEUE.md','\n## Revisão recebida após migração para D:\n\n- [ ] Fechar os platôs e ilhas com volume contínuo de terra/rocha texturizada e colisão.\n- [ ] Corrigir orientação frontal de todas as espécies, inclusive berinjelas.\n- [ ] Eliminar pico de trabalho ao liberar habilidade 2; distribuir tiros pelo mortal.\n- [ ] Habilidade 1: dois disparos perceptíveis e projéteis perfurantes distintos.\n- [ ] Habilidade 3: mira por braço, alternância rápida e pose cruzada estilizada.\n- [ ] Asas mais rápidas/sutis, amostragem contínua próxima e revisão de FPS.\n');
edit('src/game/EnemySwarm.ts','actor.visual.rotation.set(0,Math.PI,0)','actor.visual.rotation.set(0,0,0)');
edit('src/game/EnemySwarm.ts',"private procs:ItemProcs;private shadowClock", "private presentationTime=0;private procs:ItemProcs;private shadowClock");
edit('src/game/EnemySwarm.ts','this.debris.update(dt);this.effects.render(dt);','this.presentationTime+=dt;this.debris.update(dt);this.effects.render(dt);');
edit('src/game/EnemySwarm.ts','if(a.anim<(d<12?1/30:d<25?1/15:d<45?1/8:1/4))continue;', 'if(d>=24&&a.anim<(d<45?1/30:1/15))continue;');
edit('src/game/EnemySwarm.ts',"state==='Fly'?(a.time/seconds)%1", "state==='Fly'?((this.presentationTime+a.id*.073)/.44)%1");
edit('src/game/EnemySwarm.ts','((this.director.time/.75)%1)','(((this.presentationTime+a.id*.073)/.44)%1)');
edit('scripts/creature-clips.mjs','(root?.52:.23)','(root?.34:.15)');
edit('src/combat/DualPistols.ts','private stormTarget=0;', 'private stormTarget=0;private barrageIndex=14;private barrageClock=0;private barrageDirection=Vector3.Forward();private piercerDelay=-1;private piercerDirection=Vector3.Forward();');
edit('src/combat/DualPistols.ts','this.cadence.update(dt,fire&&this.visual.ready,side=>this.shoot(side));',`this.cadence.update(dt,fire&&this.visual.ready&&this.barrageIndex>=14&&this.stormRemaining<=0,side=>this.shoot(side));
    if(this.piercerDelay>=0){this.piercerDelay-=dt;if(this.piercerDelay<0)this.skillRay(1,this.piercerDirection.clone(),'double_piercer',60,true);}
    if(this.barrageIndex<14){this.barrageClock-=dt;if(this.barrageClock<=0){const i=this.barrageIndex++,angle=(i/13-.5)*1.15,dir=this.barrageDirection.clone();dir.x=this.barrageDirection.x*Math.cos(angle)+this.barrageDirection.z*Math.sin(angle);dir.z=this.barrageDirection.z*Math.cos(angle)-this.barrageDirection.x*Math.sin(angle);this.skillRay((i%2) as 0|1,dir,'backflip_barrage',24,false);this.barrageClock= .6/14;}}
`);
edit('src/combat/DualPistols.ts',"if(tier===1){for(const side of [0,1] as const)this.skillRay(side,this.camera.forward,id,60,true);return;}\n    for(let i=0;i<14;i++) {\n      const angle=(i/13-.5)*1.15;const dir=this.camera.forward.clone();\n      dir.x=this.camera.forward.x*Math.cos(angle)+this.camera.forward.z*Math.sin(angle);\n      dir.z=this.camera.forward.z*Math.cos(angle)-this.camera.forward.x*Math.sin(angle);\n      this.skillRay((i%2) as 0|1,dir,id,24,false);\n    }", "if(tier===1){this.piercerDirection.copyFrom(this.camera.forward);this.skillRay(0,this.piercerDirection.clone(),id,60,true);this.piercerDelay=.10;return;}\n    this.barrageIndex=0;this.barrageClock=0;this.barrageDirection.copyFrom(this.camera.forward);");

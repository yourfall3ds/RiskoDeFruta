; O cache do npm fica no padrão do sistema. Para isolar o cache neste projeto
; (disco cheio ou máquina compartilhada), use o Iniciar-Jogo.ps1 ou defina a
; variável npm_config_cache no ambiente, em vez de fixar um caminho absoluto aqui.
fund=false
